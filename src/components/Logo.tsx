import { Typography } from '@mui/material';

const Logo = () => (
  <Typography 
    variant="h1" 
    sx={{ 
      fontFamily: '"Punx Grotesk", Arial, sans-serif',
      // Add any other logo-specific styling
    }}
  >
    PUNX
  </Typography>
);

export default Logo; 