import { createTheme } from '@mui/material';

const theme = createTheme({
  // You can customize your theme colors and other properties here
  palette: {
    primary: {
      main: '#1976d2',
    },
  },
});

export default theme;
